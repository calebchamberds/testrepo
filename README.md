Oauth 2.0 authentication endpoint. Need to be logged out first or this fails

    POST /oauth
        grant_type
        username
        password
        client_id
        resource
        client_secret
